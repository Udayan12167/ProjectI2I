<% @wishlist=current_user.wishlist.build %>
<%= form_for(@wishlist,:remote=> true) do |f| %>
	<div>
		<%= f.text_area :link,placeholder: "Link of item..." %>
	</div>
	<div>
		<%= f.text_area :price,placeholder: "Price" %>
	</div>
	<%= f.submit "Post" , class: "btn btn-large btn-primary"%>
<% end %> 



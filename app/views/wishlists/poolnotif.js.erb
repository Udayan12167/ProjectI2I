$("#Notif-List").html("<%= j(render :partial => 'layouts/poolnotif')%>")
$("#modal-contProfile").html("<%= j(render :partial => 'wishlists/poolnotif')%>")
$("#notificaton-count").html("<%= j(render :partial => 'layouts/notifcount')%>")
$('img').on('load',function(){
  call_resize_image()
});
$('.col-md-6').hover(function () 
        {
            $(this).find('.like_button').slideDown("fast");
            $(this).find('.add_delete_button').slideDown("fast");
        },
        function ()
        {
            $(this).find('.like_button').slideUp("fast");
            $(this).find('.add_delete_button').slideUp("fast");
        }
    );

$('#basicModal').on('hidden.bs.modal', function () {
    $("#modal-contProfile").html("");
    console.log('basicModal hidden');
})
